#
# ASRC configuration
#

menu "MXC Asynchronous Sample Rate Converter support"

config MXC_ASRC
	tristate "ASRC support"
	depends on SOC_IMX53 || SOC_IMX6Q
	---help---
         Say Y to get the ASRC service.

endmenu
